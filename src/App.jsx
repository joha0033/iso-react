import React from 'react';
import { connect } from 'react-redux';


const AppDisplay = ( ) => {
    <div>
        <h1>
            ISO REACT, ISO COOL
        </h1>
    </div>
}

export default AppDisplay